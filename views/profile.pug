extends layout.pug

block content
    body
    .profile-header
        h1 #{user.firstName} #{user.lastName}
        p #{user.about}
    .profile-nav
        button.followButton Follow Me!
        a #{followers}
        a(href=`/users/:userId/about`) about
    .profile
        .about-sidebar
        .feed

    footer
                script(src="/javascripts/follow.js" type="module")
