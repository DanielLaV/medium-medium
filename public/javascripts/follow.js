const followButton = document.querySelector(".followButton");
const db = "./db/models";


followButton.addEventListener("click", (e) => {
  //   const activeUser = req.locals.user;
  //   console.log(activeUser);
  console.log(session);
  console.log("woohoo!");
});
